# Add this upstream block to your main nginx config
# and include a server block or location block as needed

upstream nancy_backend {
    server 127.0.0.1:3456;
}

# Example server block - adjust domain as needed
# server {
#     listen 80;
#     server_name nancy.yourdomain.com;
#
#     location / {
#         proxy_pass http://nancy_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 86400;
#     }
# }
